#include "iostream"
#include "cmath"

int main() { 
    double x = 3; 
    double y = 4; 
    double z;

    // z = std::max(x, y); 
    // z = std::min(x, y);
    // z = pow(x, y);
    // z = sqrt(y); 
    // z = abs(x - y); 
    // z = round(3.5); 
    // z = ceil(3.1); 
    // z = floor(3.99);

    std::cout << z << std:: endl; 

}